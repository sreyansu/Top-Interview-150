
class Solution(object):
    def maxDepth(self, root):
        if not root:
            return 0
        depth = 0
        for i in root.children:
            depth = max(depth,self.maxDepth(i))
        return 1+depth
