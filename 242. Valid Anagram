class Solution(object):
    def isAnagram(self, s, t):
        """
        :type s: str
        :type t: str
        :rtype: bool
        """
        x=''.join(sorted(s))
        y=''.join(sorted(t))
        if x==y:
            return True
        else:
            return False
