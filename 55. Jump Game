class Solution(object):
    def canJump(self, nums):
        """
        :type nums: List[int]
        :rtype: bool
        """
        maxele=0
        for i in range(len(nums)):
            if i > maxele:
                return False
            maxele=max(maxele,i+nums[i])
        return True
